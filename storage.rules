rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and write access to the score_proofs directory.
    // This is a fully permissive rule for easier uploads, as requested.
    match /score_proofs/{allPaths=**} {
      allow read;
      allow write;
    }
  }
}
