
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // This rule allows all reads and writes, regardless of authentication.
      // It is completely open and insecure, for debugging purposes only.
      allow read, write: if request.auth != null || request.auth == null;
    }
  }
}
