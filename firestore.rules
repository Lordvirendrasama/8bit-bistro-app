
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This is a temporary rule to fix deployment issues.
    // It allows all reads and writes to all collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
